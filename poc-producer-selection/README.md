## poc-producer-selection

Small proof-of-concept for random producer selection from a worker pool.

### Setup

Install dependencies from the repo root:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### Running

```bash
python3 run_poc.py
```
